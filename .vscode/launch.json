{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug (attach)",
      "type": "coreclr",
      "request": "attach",
      "processId": "${command:pickProcess}",
      "requireExactSource": false,
      "sourceFileMap": {
        "~/.am/condo/.src/**": "${workspaceRoot}/**"
      }
    }
  ]
}
