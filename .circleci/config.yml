version: 2
jobs:
  condo:
    working_directory: /target
    docker:
      - image: automotivemastermind/condo:latest
        environment:
          SKIP_NPM: 'true'
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: Run condo
          command: 'condo -- /t:publish'

workflows:
  version: 2
  build_and_test:
    jobs:
    - condo
