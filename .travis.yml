language: generic
os: linux
dist: trusty
branches:
    only:
      - develop
      - master
addons:
  apt:
    packages:
      - gettext
      - libcurl4-openssl-dev
      - libicu-dev
      - libssl-dev
      - libunwind8
      - zlib1g
      - curl
script: ./condo.sh -- /t:ci
