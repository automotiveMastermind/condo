version: '3.3'

services:
    condo:
        image: automotivemastermind/condo:unix-core1
        build:
            context: ../../
            dockerfile: ./docker/unix/Dockerfile.unix
        volumes:
            - ./../../:/app
            - ~/.nuget/:/root/.nuget/:ro
            - ~/.microsoft/usersecrets:/root/.microsoft/usersecrets
        entrypoint: ./condo.sh -- /t:publish
